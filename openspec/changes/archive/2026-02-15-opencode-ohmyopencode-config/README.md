# opencode-ohmyopencode-config

Rebaseline OpenCode integration from baseline setup to full-surface workflow parity:

- exploit OpenCode native configuration capabilities,
- leverage oh-my-opencode orchestration features intentionally,
- preserve strict no-Claude policy,
- guarantee repository-native command/skill availability through explicit OpenCode projection,
- keep `manage/with/token` operability with diagnostics.
