# Selene configuration
# https://kampfkarren.github.io/selene/
std = "lua52"

[lints]
# Allow undefined globals defined at runtime
undefined_variable = "allow"
# Allow mixed tables (required for lazy.nvim plugin specs)
mixed_table = "allow"
# Allow unscoped variables used by editor/runtime environments
unscoped_variables = "allow"
