# macOS Squirrel overrides managed by chezmoi.
# Keep frontend-specific UI behavior here.

patch:
  style/candidate_list_layout: linear
  app_options:
    com.apple.Terminal:
      ascii_mode: true
    com.mitchellh.ghostty:
      ascii_mode: true
