# bat configuration
# https://github.com/sharkdp/bat

# Use Dracula theme (matches starship and lazygit)
--theme="Dracula"

# Show line numbers and git modifications
--style="numbers,changes,header,grid"

# Enable syntax highlighting for plain text
# --map-syntax "*.conf:INI"
# --map-syntax "*.env:Bourne Again Shell (bash)"
# --map-syntax ".gitignore:Git Ignore"
# --map-syntax "*.tmpl:Go HTML Template"
--map-syntax "Justfile:Makefile"

# Use italic text (terminal must support)
--italic-text=always

# Set tab width
--tabs=4

# Wrap lines at terminal width
--wrap=auto
